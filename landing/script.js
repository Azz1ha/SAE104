document.getElementById('start-quiz')?.addEventListener('click', function(){
  // placeholder behaviour — replace with quiz flow integration
  window.alert('Quiz starting — replace with modal or navigation to quiz page.');
});
